{
  "name": "ai-committer-cli",
  "version": "1.0.0",
  "description": "A CLI tool to generate AI commit messages.",
  "main": "out/cli.js",
  "bin": {
    "ai-commit": "out/cli.js"
  },
  "scripts": {
    "compile": "tsc -p ./",
    "start": "node out/cli.js"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3"
  }
}
